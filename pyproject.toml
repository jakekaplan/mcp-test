[project]
name = "mcp-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=2.10.1",
]

[project.optional-dependencies]
test = [
    "pytest",
    "pytest-asyncio",
]
